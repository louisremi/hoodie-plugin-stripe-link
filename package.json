{
  "name": "hoodie-plugin-stripe-link",
  "version": "1.1.0",
  "description": "A simple Hoodie plugin that references a Stripe customerId in the corresponding Hoodie user",
  "main": "worker.js",
  "scripts": {
    "test": "cross-env BABEL_ENV=commonjs mocha --compilers js:babel-register --recursive --colors --timeout 10000",
    "test:watch": "npm test -- --watch"
  },
  "keywords": [
    "hoodie",
    "plugin",
    "stripe"
  ],
  "author": "@louis_remi",
  "license": "MIT",
  "dependencies": {
    "es6-promisify": "^4.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2",
    "chai": "^3.5.0",
    "cross-env": "^2.0.0",
    "mocha": "^3.0.2",
    "node-fetch": "^1.6.0"
  }
}
